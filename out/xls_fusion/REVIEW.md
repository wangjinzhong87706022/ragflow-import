# 人工评审门 · xls 融合文本（选项 B）

**待审文件（3 份）**：
- `fusion/洪水统计(1).md` —— 2018年8月21日洪水全链条（13站雨量/面雨量预报/柳林来水/水库弃水/岔口断面）
- `fusion/较大洪水统计表.md` —— 1867 年以来 49 场洪水目录 + 按年弃洪量
- `fusion/2011年下泄水量统计.md` —— 2011 三场洪水 + 全年泄水与 4 次溢流

**撰写方式**：calamine 离线读数（网格存档 `survey/extract_*.json`）+ 人工表头修复；
数值可逐值对照网格复核。行格式沿用 12 图管线约定（一行一事实、双词形、事件前缀注入每行）。

**评审通过后**：`touch approved.flag`，然后执行 `python3 rollout_xls_shell.py --apply`
（引擎已收编 `src/shell_import.py`：执行前先做 pre-flight——语料在/融合文本在/题库锚点全覆盖，
任一不满足**拒删**；然后删旧文档→重传同名 xls 壳（UNSTART 不 parse）→patch 元数据→挂融合切片
→探针验证→下一份，串行。2026-09-03 预检演练：3 份 28 切片、15 锚点零缺口）。

---

## ⚠️ 需要你裁定的两点（评审时一并定）

> **✅ 已裁定（2026-09-03）**：两点均通过——壳元数据按建议值
> `flood_event="其他", year=2018, sub_category="2018年洪水(8-21)"`（不动 src/config.py，
> 枚举扩 `2018-8` 列为后续任务）；meta_fields 污染由壳替换自然清除，无异议。

### 1. 事件归因：目录"2008年洪水"实为 2018年8月21日洪水

证据四条（`survey/SURVEY.md`）：表内标题"20180821"、Excel 序列日 43333=2018-08-21、
《较大洪水统计表》180821 行逐值互证、目录 2007→2010 间无 2008 场次。

- **3 份融合文本已按真值 2018 年撰写**；
- KB 元数据待定：`洪水统计(1).xls` 现为 `flood_event="2008-8", year=2008`（错）。
  壳替换时我建议改为 `flood_event="其他", year=2018, sub_category="2018年洪水(8-21)"`——
  **不改 src/config.py**（枚举加 "2018-8" 需动 config+测试，超出本批"src/ 零改动"约定；
  如你同意扩枚举，作为后续单独小任务处理）。
- 同目录另 2 份未动桩的 naive 文档（降雨量统计(1).xls、弃水计算.xls）元数据同为 2008-8，
  本批不触碰，可日后批量纠偏。

### 2. meta_fields 污染顺带治愈

`洪水统计(1).xls` 现有 meta_fields 被解析器自动抽取污染（`Unnamed: 11`、整列值当字段名）。
壳替换后新文档只写 11 字段规范元数据，污染自然清除（较大洪水统计表/2011表本来干净，原样拷贝）。

## 验收计划（迁移后自动执行）

- 12 题 before 6/12 → after 目标 ≥10/12；目标文档 top1 占比显著提升
- 探针：瑶曲 0/3→3/3、1353 0/3→3/3、10561 保持 3/3
- 全量回归：QC 六问 6/6 + 26 题 22/26 持平；audit_chunks.py 复验
- 回滚：重传 naive 解析（已验证可精确还原块数）+ 删前全量块存档 `xls_chunks_archive.json`
