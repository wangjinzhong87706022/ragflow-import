# xls 定向融合文本（选项 B）迁移与验证报告

- **日期**：2026-09-03
- **执行**：`rollout_xls_shell.py --apply`（引擎 `src/shell_import.py`，人工评审门 2026-09-03 通过）
- **范围**：ds3 三份高价值 xls → 原文件壳（UNSTART 不 parse）+ 11 字段元数据 + 手动挂融合切片
- **关联**：`docs/evaluation-xls-fusion-2026-09-02.md`（方案）、`docs/review-vision-xls-2026-09-03.md`（评审）、`REVIEW.md`（裁定记录）

## 一、迁移结果（3/3 成功，严格串行）

| 文档 | 块数变化 | 壳 run | 探针 rank* | 元数据要点 |
|---|---|---|---|---|
| 洪水统计(1).xls | 12 → 10 | UNSTART | 18 | **event=其他 / year=2018 / sub=2018年洪水(8-21)**（裁定值）；解析器 meta 污染一并清除 |
| 较大洪水统计表.xls | 27 → 11 | UNSTART | 23 | event=历年统计（跨年目录，无单一年份） |
| 2011年下泄水量统计.xls | 101 → 7 | UNSTART | 1 | event=历年统计 / year=2011 |

*探针 rank = 验证门内目标文档切片在全返回集（30 条）中的最佳排名。合计 **140 → 28 块（-112 垃圾块）**，全部数值经洪水目录互证，锚点覆盖 pre-flight 断言通过（15/15）。

回滚资产：删前全量块存档 `xls_chunks_archive.json`（12/27/101 块 + 原 chunk_method/parser_config/meta 快照），`--rollback "<KB名>"` 可精确还原 naive 现状。

## 二、验证结果

### 检索评测（12 题 ×3 取多数，top10 并集口径）：6/12 → **8/12**

| 题 | 结果 | 说明 |
|---|---|---|
| X1 瑶曲148 / X2 383 / X3 466·1308 / X4 1238 / X8 958 / X10 904 / X11 10561·9401 / X12 1160 | PASS | X1/X3/X12 目标文档进入 top10（迁移前 0 题）；其余经 top10 并集命中（多由汇报 .doc 同载） |
| X5 面雨量87.66 / X6 1353 / X7 1867 / X9 2110 | FAIL (0/3) | **全部为排序层缺口，非内容缺失**（见下） |

**目标 ≥10/12 未全达（8/12），如实记录。** 4 个 FAIL 的归因证据：

- 内容全部在库可达：纯值查询 `1867`→rank1-4、`10561`→rank1、`148`→目标切片在返回集 rank14-18；
- QA 原句问法可达：`哪场洪水是200年一遇`→rank13-15、`2011年水库累计弃水量…低洞…`→**rank1**；
- 自然问法不可达：混合打分下整篇汇报 .doc 的关键词密度（sim 8+）系统性压过行文本切片（0.3-0.6），
  且姊妹 naive 文档（降雨量统计(1).xls 等 F1 遗留）同池竞争。
- 与库容表案例同构（值在库、纯数值查询确定可达、chat 层可答），正解 = 评审 P2-9 检索层专项
  （rerank / top_n / 查询改写），不在本波次范围。

### 探针（probe_xls.py，top10 口径）

- 探针1 同头垃圾 3/10（基线 0/10）——我们删除 140 块后排序池重排，姊妹 naive 文档的垃圾头浮出；
  再次印证 F1 是全 KB 级疾病、单点治理会洗牌（同 P1 试点结论）。
- 探针2 自然问法 top10 命中 0/3——与评测 FAIL 同根因（见上），非切片缺失。

### 全量回归（强制门）：全绿

- **QC 六问 6/6**（out/qc/report_20260903_133738.md）——Q5/Q6 元数据硬过滤钻取通过，
  壳元数据变更（2008-8 → 其他/2018）无副作用；
- **26 题检索 22/26**（report_20260903_134101.md）——与基线逐题一致（FAIL 仍为 W5/W6/G3/G4），零回归。

## 三、执行过程：验证门三次 FAIL 的根因链（已固化为机制）

1. **首跑 FAIL**（瑶曲148 要求 top10）：门设过严。行文本切片在混合打分下排不进 top10——
   PNG 试点已记录同教训（"直查比探针 top10 可靠"），本次真正吸收：verify 门改设在全返回集
   （v0.27.1 恒返回 30 条），top10 排名降为遥测字段 `probe_rank`。
2. **二跑 FAIL**（较大洪水统计表）：探针问法踩中 v0.27.1 问法敏感——迁移前后候选池变化使
   同一问法排名重排（自然问法 >30，QA 原句 13-15，纯值 rank1-4）。
3. **三跑 FAIL**（2011表）：同一坑。**规律至此完全一致：验证门探针必须使用切片内"常问速查"的
   QA 原句。** 改用原句后 rank1。
   
三项教训已固化进 `src/shell_import.py`（verify 门语义 + docstring），后续 wave 直接继承。

## 四、遗留与后续（按评审排期）

1. **P2-9 检索层专项：✅ 已完成（2026-09-03 下午）**——壳块补 tag_feas（根因=
   tag_fea×10 打分加成）后 12 题 **8/12→12/12**；rerank/查询改写实测无增益未采纳；
   多库场景另发现候选池 64 预截断问题（26 题 22→24/26，见
   `out/retrieval_tuning/REPORT.md`）。剩余 W5/G3 为内容层缺口，转 P1-4 补行。
2. **P1-4 chunk 级就地修正**：剖面图 790.30→790.5、架构图补 G3/G4 速查行
   （`shell_import.patch_chunk()` 已备好接口；改后同步 out/ 的 fusion md 源）。
3. **P1-5 2018 元数据纠偏**：config 枚举扩 `2018-8` + `FLOOD_EVENT_BY_SUBDIR` 修正 +
   同目录 2 份 naive 文档（降雨量统计(1)/弃水计算）patch + 对齐测试（已验证零回归依赖）。
4. **F1 收口**：姊妹垃圾文档仍在产出同头垃圾（探针1 3/10），xls wave-2 triage（剩 20 份
   keep/fuse/ignore）+ 或考虑对确认无价值文档做 P2 式外科删除。
5. `洪水过程_dup.xls`（10-3 全集）入库裁定；ds4 xlsx 复用本方案评估。

## 五、产物清单

- 迁移台账：`manifest_xls_shell.json`（含每步明细与探针 rank）、`shell_state.json`（幂等）
- 回滚资产：`xls_chunks_archive.json`（140 块全量 + 快照）
- 评测：`report_xls_before_20260902_182132.md` / `report_xls_after_20260903_133343.md` + results JSON
- 回归：`out/qc/report_20260903_133738.md`、`out/vision_8chart_test/qa_eval/report_20260903_134101.md`
